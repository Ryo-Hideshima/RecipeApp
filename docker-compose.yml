services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: recipeapp
      MYSQL_USER: recipeapp
      MYSQL_PASSWORD: recipeapp
      MYSQL_ROOT_PASSWORD: recipeapp
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
